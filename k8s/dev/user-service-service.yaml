apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  type: LoadBalancer
  ports:
    - name: "8700"
      port: 8700
      targetPort: 8700
  selector:
    app: user-service
status:
  loadBalancer: {}
